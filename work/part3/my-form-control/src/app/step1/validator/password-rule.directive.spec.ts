import { PasswordRuleDirective } from './password-rule.directive';

describe('PasswordRuleDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordRuleDirective();
    expect(directive).toBeTruthy();
  });
});
